/**
 * Shared preset props for `AppHeader` to keep header UI consistent across pages.
 * Keep these values aligned with the current "พร้อมขาย" header UI.
 */
export const APP_HEADER_PRESET = {
  controlSize: 40,
  iconSize: 22,
} as const;

