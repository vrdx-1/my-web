'use client';

/**
 * Search History admin page — ฟีเจอร์การค้นหาถูกลบออกแล้ว
 */
export default function AdminSearchHistoryPage() {
  return (
    <div style={{ padding: '24px' }}>
      <h1 style={{ fontSize: '24px', marginBottom: '12px' }}>Search History</h1>
      <p style={{ color: '#666' }}>ฟีเจอร์การค้นหาถูกลบออกแล้ว — หน้านี้ไม่มีการใช้งาน</p>
    </div>
  );
}
